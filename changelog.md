## Bypasser_v2026091003

- Adapt to Magisk 31000. 
- Support fetching the latest generator from GitHub via the web UI. 
- **Users using earlier versions on Magisk 31000 or above must flash this version of the module to make the shell scripts available.** 

---

## Bypasser_v2026080202

- Adjust the variable order. 
- Optimize the action scripts and the web UI. 

---

## Bypasser_v2026062002

- Adjust the logic of ``action.sh``. 
- Update the web UI. 
- Adjust to update only the specified generator. 

---

## Bypasser_v2026042806

- Implement the game of cat and mouse. 
- Introduce C++ into the module. 
- Add support to generate HMA-OSS v93 configurations. 
- Bump the Python script for Regular Update to ``synchronizeDatabase.py`` with more rigorous logic. 

---

## Bypasser_v2026020606

- Separate the Tricky Store ``target.txt`` configuration to avoid applications that cannot be in ``target.txt``. 
- Optimize professional statements and up-to-date solutions. 
- Fix potential bugs caused by hurried regular updates. 
- Rename ``Update.json`` to ``update.json`` as most rooting-layer system modules do. 
- Enable brief workflows in GitHub Actions. 
- Happy CNY! 
- **Users using earlier versions must flash this version of the module to make the module update available in the root manager.** 

---

## Bypasser_v2025122018

- Update the suggestions. 
- Bump TMLP to LRFP. 

---

## Bypasser_v2025070916

- Adjust the order and the exit code of each bypassing block. 
- Optimize the logic of wiping the Zygisk traces. 
- Add more shell functions. 
- Optimize the logic of the permission setting. 
- Optimize the plain string searching. 

---

## Bypasser_v2025052822

- Store the classification cache files locally to avoid issues when there is no network connection. 
- Support showing the classifications in the web UI. 
- Make the prompts in the web UI colorful. 

---

## Bypasser_v2025052503

- Optimize the logic of handling the Zygisk traces. 
- Adjust the exit codes of the scripts related to ``action.sh``. 

---

## Bypasser_v2025042223

- Adjust logic of the ``action`` for Zygisk traces. 
- Change the location of the action configuration file. 
- Enhance the robustness of the ``action.sh``. 
- **Users using earlier versions must flash this version of the module to make the dynamic updating for ``action.sh`` and web UI available.** 

---

## Bypasser_v2025022219

- Make printing, cache clearing, working directory changing, and permission setting systematic based on the stack thought. 
- Optimize the logic of the scripts and the web UI. 

---

## Bypasser_v2025022023

- Further adapt the ``action.sh`` to Apatch. 
- **Users using earlier versions must update ``action.sh`` by flashing this version of the module to make the dynamic updating of ``action.sh`` available.** 

---

## Bypasser_v2025022022

- Improve the loop logic. 
- Further adapt to the module installer of Apatch. 
- Support showing the action slot in the web UI. 

---

## Bypasser_v2025021823

- Support updating the web UI dynamically. 
- Adjust the logic of the ``action.sh``. 

---

## Bypasser_v2025021723

- Implement the web UI, though there is nothing to display at this time. 
- Add configuration codes for Zygisk Next. 
- Adapt to the shell interpreter of Apatch better. 

---

## Bypasser_v2025012617

- Perform local shell syntax checks in the builder and the installer. 
- Support asking users for willingness to perform local scanning before executing the ``action.sh`` in the installer of the root manager. 
- Implement better time consumption computation. 
- Adjust the exit codes of the scripts. 

---

## Bypasser_v2025012523

- Format commit messages. 
- Change updating behaviors in ``action.sh``. 
- Manage permissions in a serious way. 
- Download the latest version of ``action`` only when the SHA-512 value of the local ``action.sh`` is different from that in the latest SHA-512 file. 
- Use SHA-512 file to verify the dynamic update of ``action.sh``. 
- Optimize the exit codes of ``action.sh``. 
- Make the local application scanning optional. 
- Adjust the format of the function declaration in all the scripts. 
- **Users using earlier versions must update ``action.sh`` by flashing this version of the module to make the dynamic updating of ``action.sh`` available.** 

---

## Bypasser_v2025012422

- Support checking the manager version before installation. 
- Support checking SHA-512 values before installation. 

---

## Bypasser_v2025011505

- Optimize the logic of the script ``action.sh``. 
- Design the script ``customize.sh``. 

---

## Bypasser_v2025011223

- Support creating the Shamiko whitelist configuration file. 
- Implement automatic ``action.sh`` updating. 

---

## Bypasser_v2025011023

- Support generating the configurations for HMA/HMAL. 
- Support generating the configurations for the Tricky Store target file. 

---

## Bypasser_v2025010922

- Establish the repository architecture and made it ready as a template. 
- Support updating via the root manager. 

---

## Bypasser_v2025010900

- This is the initial commit of the Bypasser rooting-layer system module. 
- This module has its own name and direction from now on. 
