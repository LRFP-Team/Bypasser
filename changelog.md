## Bypasser_v2026050113

- Test

## Bypasser_v2026042806

- Implemented the game of cat and mouse. 
- Introduced C++ into the module. 
- Added support to generate HMA-OSS v93 configurations. 
- Bumped the Python script for Regular Update to ``synchronizeDatabase.py`` with more rigorous logic. 

---

## Bypasser_v2026020606

- Separated the Tricky Store ``target.txt`` configuration to avoid applications that cannot be in ``target.txt``. 
- Optimized professional statements and up-to-date solutions. 
- Fixed potential bugs caused by hurried regular updates. 
- Renamed ``Update.json`` to ``update.json`` as most rooting-layer system modules do. 
- Enabled brief workflows in GitHub Actions. 
- Happy CNY! 

---

## Bypasser_v2025122018

- Updated the suggestions. 
- Bumped TMLP to LRFP. 

---

## Bypasser_v2025070916

- Adjusted the order and the exit code of each bypassing block. 
- Optimized the logic of wiping the Zygisk traces. 
- Added more shell functions. 
- Optimized the logic of the permission setting. 
- Optimized the plain string searching. 

---

## Bypasser_v2025052822

- Stored the classification cache files locally to avoid no network connection. 
- Supported showing the classifications in the web UI. 
- Made the prompts in the web UI colorful. 

---

## Bypasser_v2025052503

- Optimized the logic of handling the Zygisk traces. 
- Adjusted the exit codes of the scripts related to ``action.sh``. 

---

## Bypasser_v2025042223

- Adjusted logic of the ``action`` for Zygisk traces. 
- Changed the location of the action configuration file. 
- Enhanced the robustness of the ``action.sh``. 
- Users using earlier versions must flash this version of the module to make the dynamic updating for ``action.sh`` and web UI available. 

---

## Bypasser_v2025022219

- Made printing, cache clearing, working directory changing, and permission setting systematic based on the stack thought. 
- Optimized the logic of the scripts and the web UI. 

---

## Bypasser_v2025022023

- Further adapted the ``action.sh`` to Apatch. 
- Users using earlier versions must update ``action.sh`` by flashing this version of the module to make the dynamic updating for ``action.sh`` available. 

---

## Bypasser_v2025022022

- Improved the loop logic. 
- Further adapted to the module installer of Apatch. 
- Supported showing the action slot in the web UI. 

---

## Bypasser_v2025021823

- Supported updating the web UI dynamically. 
- Adjusted the logic of the ``action.sh``. 

---

## Bypasser_v2025021723

- Implemented the web UI though there is nothing to display at this time. 
- Added configuration codes for Zygisk Next. 
- Adapted to the shell interpreter of Apatch better. 

---

## Bypasser_v2025012617

- Performed local shell syntax checks in the builder and the installer. 
- Supported asking users for the local scanning willingness before executing the ``action.sh`` in Magisk installer. 
- Implmeneted better time consumption computation. 
- Adjusted the exit codes of the scripts. 

---

## Bypasser_v2025012523

- Formatted commit messages. 
- Changed updating behaviors in ``action.sh``. 
- Managed permissions in a serious way. 
- Downloaded the latest version of ``action`` only when the SHA-512 value of the local ``action.sh`` is different from that in the latest SHA-512 file. 
- Used SHA-512 file to verify the dynamic update of ``action.sh``. 
- Optimized the exit codes of ``action.sh``. 
- Made the local application scanning optional. 
- Adjusted the format of the function declaration in all the scripts. 
- Users using earlier versions must update ``action.sh`` by flashing this version of the module to make the dynamic updating for ``action.sh`` available. 

---

## Bypasser_v2025012422

- Supported checking the manager version before installation. 
- Supported checking SHA-512 values before installation. 

---

## Bypasser_v2025011505

- Optimized the logic of the script ``action.sh``. 
- Designed the script ``customize.sh``. 

---

## Bypasser_v2025011223

- Supported creating the Shamiko whitelist configuration file. 
- Implemented automatic ``action.sh`` updating. 

---

## Bypasser_v2025011023

- Supported generating the configurations for HMA/HMAL. 
- Supported generating the configurations for the Tricky Store target file. 

---

## Bypasser_v2025010922

- Established the repository architecture and made it ready as a template. 
- Supported updating via the Magisk Manager. 

---

## Bypasser_v2025010900

- This is the initial commit of the Bypasser rooting-layer system module. 
- This module has its own name and direction from now on. 
